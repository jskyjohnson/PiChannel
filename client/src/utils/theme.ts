import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#9c27b0",
      light: "#ea80fc",
    },

    secondary: {
      main: "#69f0ae",
    },
  },
});

export default theme;
